includes:
    - phar://phpstan.phar/conf/bleedingEdge.neon

parameters:
    paths:
        - app
        - bootstrap/app.php
        - config
        - database
        - routes
        - modules
        - tests

    excludePaths:
        - modules/*/Tests/*

    level: max

    checkConfigTypes: true

    stubFiles:
        - tests/.phpstan/Pipeable.stub

    ignoreErrors:
        # Pest arch describe() blocks with arch() expectations are valid patterns
        - identifier: pest.describe.withoutTests

        # Pest arch internal API (ignoring()) used intentionally
        - identifier: method.internalInterface

        # Type coverage is enforced by php artisan test --type-coverage
        - identifier: typeCoverage.paramTypeCoverage

    # Larastan optional rules
    checkModelProperties: true
    checkModelMethodVisibility: true
    noUnnecessaryEnumerableToArrayCalls: true
    generalizeEnvReturnType: true
    checkMissingTranslations: true
    parseModelCastsMethod: true
    checkAuthCallsWhenInRequestScope: true

    # Module migration paths for schema inference
    databaseMigrationsPath:
        - modules/*/Database/migrations

    # Translation files (Laravel 13 uses lang/, not resources/lang/)
    translationDirectories:
        - lang

    # Cache parsed migrations between runs
    enableMigrationCache: true

    parallel:
        maximumNumberOfProcesses: 4
        minimumNumberOfJobsPerProcess: 4
        jobSize: 20

    phpVersion: 80400
