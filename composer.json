{
    "$schema": "https://getcomposer.org/schema.json",
    "name": "thomiomi/laravel-starterkit-api",
    "type": "project",
    "description": "Laravel Starter Kit API — modular monolith with Sanctum auth, Spatie permissions, Socialite, and Pennant feature flags.",
    "keywords": [
        "laravel",
        "framework"
    ],
    "license": "MIT",
    "require": {
        "php": ">=8.4.1",
        "dedoc/scramble": "^0.13.30",
        "laravel/framework": "^13.6.0",
        "laravel/pennant": "^1.23.0",
        "laravel/prompts": "^0.3.21",
        "laravel/sanctum": "^4.0",
        "laravel/socialite": "^5.0",
        "laravel/tinker": "^3.0",
        "spatie/laravel-permission": "^8.0"
    },
    "require-dev": {
        "captainhook/captainhook": "^5.29",
        "fakerphp/faker": "^1.23",
        "larastan/larastan": "^3.9",
        "laravel/boost": "^2.2",
        "laravel/pail": "^1.2.5",
        "laravel/pao": "^1.0",
        "laravel/pint": "^1.27",
        "mockery/mockery": "^1.6",
        "nunomaduro/collision": "^8.6",
        "pestphp/pest": "5.0.2",
        "pestphp/pest-plugin-agent": "5.0",
        "pestphp/pest-plugin-laravel": "5.0.1",
        "pestphp/pest-plugin-phpstan": "^5.0",
        "pestphp/pest-plugin-profanity": "5.0",
        "pestphp/pest-plugin-rector": "^5.0",
        "pestphp/pest-plugin-type-coverage": "5.0",
        "phpstan/extension-installer": "^1.4",
        "phpstan/phpstan-deprecation-rules": "^2.0",
        "phpstan/phpstan-mockery": "^2.0",
        "rector/rector": "^2.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Modules\\": "modules/",
            "Database\\Factories\\": "database/factories/",
            "Database\\Seeders\\": "database/seeders/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "setup": [
            "composer install",
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\"",
            "@php artisan key:generate",
            "@php artisan migrate --force"
        ],
        "setup:ci": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\"",
            "@php artisan key:generate",
            "@php -r \"file_exists('database/database.sqlite') || touch('database/database.sqlite');\"",
            "@php artisan migrate --force"
        ],
        "dev": [
            "Composer\\Config::disableProcessTimeout",
            "@php artisan dev"
        ],
        "lint": [
            "pint --parallel"
        ],
        "lint:staged": [
            "pint --parallel --dirty"
        ],
        "lint:check": [
            "pint --parallel --test"
        ],
        "types:check": [
            "phpstan analyse --memory-limit=512M"
        ],
        "test": [
            "@php artisan config:clear --ansi",
            "@lint:check",
            "@types:check",
            "@php artisan test"
        ],
        "test:quality": [
            "@php artisan config:clear --ansi",
            "@lint:check",
            "@types:check",
            "@php artisan test --ci --coverage --type-coverage --min=100 --memory-limit=512M"
        ],
        "test:mutation": [
            "@php artisan test --mutate --min=100"
        ],
        "test:profanity": [
            "@php artisan test --profanity"
        ],
        "test:snapshot": [
            "@php artisan test --update-snapshots"
        ],
        "test:tia": [
            "@php artisan test --tia"
        ],
        "ci:check": [
            "Composer\\Config::disableProcessTimeout",
            "@test:quality",
            "@rector:dry",
            "@test:profanity"
        ],
        "rector:dry": [
            "rector --dry-run"
        ],
        "rector": [
            "rector"
        ],
        "release": [
            "npx @grazulex/shipmark release"
        ],
        "release:dry": [
            "npx @grazulex/shipmark release --ci auto --dry-run"
        ],
        "post-autoload-dump": [
            "Illuminate\\Foundation\\ComposerScripts::postAutoloadDump",
            "@php artisan package:discover --ansi"
        ],
        "post-update-cmd": [
            "@php artisan vendor:publish --tag=laravel-assets --ansi --force",
            "@php artisan boost:update --ansi"
        ],
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\""
        ],
        "post-create-project-cmd": [
            "@php artisan key:generate --ansi",
            "@php -r \"file_exists('database/database.sqlite') || touch('database/database.sqlite');\"",
            "@php artisan migrate --graceful --ansi"
        ],
        "pre-package-uninstall": [
            "Illuminate\\Foundation\\ComposerScripts::prePackageUninstall"
        ]
    },
    "scripts-descriptions": {
        "setup": "Install dependencies and prepare the application",
        "setup:ci": "Prepare the application for CI (copy .env, key:generate, create sqlite, migrate)",
        "dev": "Run all dev processes concurrently (server, queue, logs)",
        "lint": "Automatically fix code style using Laravel Pint",
        "lint:staged": "Auto-fix code style for staged files (pre-commit)",
        "lint:check": "Check code style without modifications",
        "types:check": "Run PHPStan static analysis and type coverage",
        "test": "Run lint, static analysis, and the test suite",
        "test:quality": "Run lint, static analysis, and tests with code and type coverage (full suite, TIA skipped)",
        "test:mutation": "Run mutation testing to validate test suite quality",
        "test:profanity": "Check tests for prohibited words",
        "test:snapshot": "Update stored snapshots (tests/.pest/snapshots)",
        "test:tia": "Re-run only tests affected by your changes (test impact analysis); also enabled automatically on local machines",
        "ci:check": "Run the full CI pipeline (quality, refactoring, and profanity)",
        "release": "Create a new release changelog",
        "release:dry": "Preview the next release changelog without committing"
    },
    "extra": {
        "laravel": {
            "dont-discover": []
        },
        "phpstan": {
            "includes": [
                "extension.neon"
            ]
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true,
            "phpstan/extension-installer": true
        },
        "platform": {
            "php": "8.4.22"
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
