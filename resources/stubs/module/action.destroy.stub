<?php

declare(strict_types=1);

namespace Modules\{{Module}}\Actions;

use Illuminate\Database\DatabaseManager;
use Modules\{{Module}}\Models\{{Module}};
use Modules\{{Module}}\Repositories\{{Module}}Repository;

/**
 * Action for deleting a {{lowerResource}}.
 */
final readonly class Destroy{{Module}}Action
{
    /**
     * Create a new Destroy{{Module}}Action instance.
     */
    public function __construct(
        private DatabaseManager $database,
        private {{Module}}Repository $repository
    ) {}

    /**
     * Execute the delete {{lowerResource}} action.
     *
     * @param  {{Module}}  ${{lowerResource}}  The {{lowerResource}} model instance.
     * @return bool True if the {{lowerResource}} was deleted successfully, false otherwise.
     */
    public function handle({{Module}} ${{lowerResource}}): bool
    {
        return $this->database->transaction(fn () => $this->repository->delete(${{lowerResource}}));
    }
}
