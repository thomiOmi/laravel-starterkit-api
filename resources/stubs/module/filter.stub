<?php

declare(strict_types=1);

namespace Modules\{{Module}}\Filters;

use App\Support\Filters\BaseFilter;
use Modules\{{Module}}\Models\{{Module}};

/**
 * @extends BaseFilter<{{Module}}>
 */
class {{Module}}Filter extends BaseFilter
{
    /** @var array<int, string> */
    protected array $allowedFilters = [
{{filterFields}}    ];

    /** @var array<int, string> */
    protected array $allowedSorts = [
{{filterSorts}}    ];

    /** @var array<int, string> */
    protected array $allowedFields = [
        'id',
{{filterAllowedFields}}{{filterTimestamps}}    ];

    /** @var array<int, string> */
    protected array $searchableColumns = [
{{filterSearchableColumns}}    ];

    // Strategy methods for complex filters (e.g. relation filters, custom logic)
    // delegate to Model scopes using #[Scope] attribute (Laravel 13+)
}
