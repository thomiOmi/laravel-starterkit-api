<?php

declare(strict_types=1);

namespace Modules\{{Module}}\Actions;

use Illuminate\Contracts\Pagination\Paginator;
use Modules\{{Module}}\Filters\{{Module}}Filter;
use Modules\{{Module}}\Models\{{Module}};
use Modules\{{Module}}\Repositories\{{Module}}Repository;

/**
 * Action for listing paginated {{labelPlural}} with filters.
 */
final readonly class List{{pluralModule}}Action
{
    public function __construct(
        private {{Module}}Repository $repository
    ) {}

    /**
     * Execute the list {{labelPlural}} action.
     *
     * @param  {{Module}}Filter  $filter  The {{label}} filter instance.
     * @param  int  $perPage  Number of items per page.
     * @return Paginator<int, {{Module}}> The paginated {{labelPlural}}.
     */
    public function handle({{Module}}Filter $filter, int $perPage = 10): Paginator
    {
        return $this->repository->paginate($filter, $perPage);
    }
}
