<?php

declare(strict_types=1);

namespace Modules\{{Module}}\Payloads\{{Version}};

use Modules\{{Module}}\Requests\{{Version}}\{{Action}}{{Module}}Request;

/**
 * Payload for {{Action}} {{Module}} data.
 */
final readonly class {{Action}}{{Module}}Payload
{
    public function __construct(
{{payloadFields}}    ) {}

    /**
     * Create a payload from the request.
     */
    public static function fromRequest({{Action}}{{Module}}Request $request): self
    {
        return new self(
{{payloadFromRequest}}        );
    }

    /**
     * Convert the payload to an array for Eloquent.
     *
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        return [
{{payloadToArray}}        ];
    }
}
