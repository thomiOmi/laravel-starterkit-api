<?php

declare(strict_types=1);

namespace Modules\{{Module}}\Actions;

use Modules\{{Module}}\Models\{{Module}};

/**
 * Action for retrieving a single {{label}} profile.
 */
final readonly class Show{{Module}}Action
{
    /**
     * Execute the show {{label}} action.
     *
     * @param  string  $id  The {{label}} ID.
     * @return {{Module}}|null The {{label}} instance or null if not found.
     */
    public function handle(string $id): ?{{Module}}
    {
        return {{Module}}::query()->find($id);
    }
}
